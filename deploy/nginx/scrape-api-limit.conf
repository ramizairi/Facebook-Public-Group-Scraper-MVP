# Copy this file to /etc/nginx/conf.d/scrape-api-limit.conf on Debian.
# It is loaded inside the `http {}` block by the default nginx.conf include.

limit_req_zone $binary_remote_addr zone=scrape_api_per_ip:10m rate=10r/m;
limit_conn_zone $binary_remote_addr zone=scrape_api_conn:10m;
