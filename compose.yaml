services:
  scraper:
    build:
      context: .
      dockerfile: Dockerfile
    image: facebook-public-group-scraper
    user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
    stdin_open: true
    tty: true
